JsonArray jsonArray = Json.parse(input).asArray();
JsonArray jsonArray = Json.parse(line).asArray();

return Json.parse("[" + input + "]").asArray().get(0).asString();


/**
 * <a href=""><h1></h1></a>
 *
 * @author Jacob Suen
 * @since ${HOUR}:${MINUTE} ${MONTH_NAME_SHORT} ${DAY}, ${YEAR}
 */
class ${NAME}_Solution {
}


/**
 * @author LeetCode Playground
 */
public class ${NAME} {
}