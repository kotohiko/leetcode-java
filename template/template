JsonArray jsonArray = Json.parse(line).asArray();
return Json.parse("[" + input + "]").asArray().get(0).asString();


/**
 * <h2></h2>
 * <a href="">LeetCode link</a>
 *
 * @author Jacob Suen
 * @since ${HOUR}:${MINUTE} ${MONTH}/${DAY}/${YEAR}
 */
class ${NAME} {
}


/**
 * @author LeetCode Playground
 */
public class ${NAME} {
}